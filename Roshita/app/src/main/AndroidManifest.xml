<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mohamed.roshita">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="روشـتـة"
        android:logo="@mipmap/logo"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Client1Activity" />
        <activity android:name=".Seller1Activity" />
        <activity android:name=".Client3Activity" />
        <activity android:name=".Client4Activity" />
        <activity android:name=".Client5Activity" />
        <activity android:name=".Client6Activity" />
        <activity android:name=".Seller3Activity" />
        <activity android:name=".Seller4Activity" />
        <activity android:name=".Seller5Activity" />
        <activity android:name=".Seller6Activity" />
        <activity android:name=".Seller7Activity" />
        <activity android:name=".Seller8Activity" />
        <activity android:name=".Seller9Activity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".SellerMap" />
        <activity
            android:name=".ClientSelectMap"
            android:label="@string/title_activity_client_select_map" />
        <activity
            android:name=".ResultPharmLocation"
            android:label="@string/title_activity_result_pharm_location" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".Client7Activity" />
        <activity android:name=".ClientInboxActivity" />
        <activity android:name=".Seller10Activity" />
        <activity android:name=".Client8Activity" />
        <activity android:name=".Seller11Activity"/>
    </application>

</manifest>